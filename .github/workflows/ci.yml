name: CI
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install deps (ai-service)
        run: |
          python -m pip install --upgrade pip
          pip install fastapi uvicorn
      - name: Run ai-service health check
        run: echo "CI placeholder - add tests later"
